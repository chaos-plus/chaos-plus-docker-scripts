

networks:
  default:
    name: ${NETWORK}
    external: true

services:

  redis:
    image: redis:7
    restart: ${SRV_RESTART_MODE:-always}
    ports:
      - "${PORT_REDIS:-6379}:6379"
    environment:
      - PASSWORD=${PASSWORD:-}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DATA}/redis:/data
    command: 
      - redis-server 
      - '--port 6379'
      - '--requirepass "${PASSWORD:-}"'
      - '--tcp-keepalive 50'
      - '--appendonly yes'
      - '--appendfsync everysec'
      #- '--stop-writes-on-bgsave-error no'
      - '--save 60 1'
      - '--bind 0.0.0.0'
      - '--protected-mode yes'
    healthcheck:
      test: ["CMD-SHELL", "redis-cli --no-auth-warning -p 6379 -a \"${PASSWORD:-}\" ping | grep -q PONG"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    deploy:
      # endpoint_mode: dnsrr
      # restart_policy:
      #   condition: ${SRV_RESTART_MODE:-any}
      # placement:
      #   constraints:
      #     - node.labels.app_redis == true
      resources:
        limits:
          memory: ${RES_LIMIT_MEM_REDIS:-1G}
        reservations:
          memory: ${RES_RESER_MEM_REDIS:-256M}

